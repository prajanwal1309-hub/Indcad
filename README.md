# IndCad — NOC Matching (drop-in)

Place these files into a single folder `indcad/`.

## Quick start (local)
1. Create venv and activate
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
